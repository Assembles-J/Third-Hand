"""Use-case orchestration layer for v2 modules.

This package is intentionally named ``application_services`` while the legacy
``app.application`` module still exists.  It may be renamed after the strangler
migration removes that module.
"""
